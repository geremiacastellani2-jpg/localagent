# Nucleo — solo dipendenze con wheel universali, si installa ovunque.
# (uvicorn "liscio", senza gli extra [standard] che tirano dentro uvloop/httptools
#  e possono non avere wheel sulle versioni di Python più recenti.)
fastapi>=0.111
uvicorn>=0.30
openai>=1.40
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.7
